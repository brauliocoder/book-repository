var item = document.querySelector("<%= escape_javascript("#btn-#{@reserved_book.book_id}") %>");
if (item){
  item.remove();
  var card_menu = document.querySelector("<%= escape_javascript("#body-#{@reserved_book.book_id}") %>")
  if (card_menu) {
    card_menu.insertAdjacentHTML("beforeend", "<%= escape_javascript( render 'books/components/disabled_button' ) %>");
  }
}
